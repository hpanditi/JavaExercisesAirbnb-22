// bad
//is current tab
const active = true;

// good
// is current tab
const active = true;

// bad
/**
 * make() returns as a new element
 * based on the passed-in tag name
 */
function make(tag) {

    // ...

    return Element;
}

// good
/**
 * make() returns as a new element
 * based on the passed-in tag name
 */
function make(tag) {

    // ...

    return Element;
}