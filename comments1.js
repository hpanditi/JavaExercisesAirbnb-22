// bad
// make() returns as a new element
// based on the passed in tag name
//
// @param {String} tag
// @return {Element} element
function make(tag) {

    // ...

    return element;
}

// good
/**
    * make() returns as a new element
    * based on the passed-in tag name
    * /
function make(tag) {

    // ...

    return element;
}